receivers:
  otlp:
    protocols:
      grpc:
      http:
exporters:
  zipkin:
    endpoint: "http://zipkin-all-in-one:9411/api/v2/spans"
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [zipkin]
  telemetry:
    logs:
      level: "debug"